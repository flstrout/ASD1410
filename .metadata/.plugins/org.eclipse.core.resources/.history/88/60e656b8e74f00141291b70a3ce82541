var netCheck = function(){
	var url = "http://api.reddit.com/r/startrek";
	
	/*var loadData = function (e){
		var data = this.responseText;
		console.log("Is this working");
	};
	var errorData = function (e){
		alert("Error: " + e.error);
	};*/
	
	if (Ti.Network.online){
		var dataSource = Ti.Network.createHTTPClient({
			onload: function (e){
				var myData = this.responseText;
				console.log(myData);
	},
			onerror: function (e){
				alert("Error: " + e.error);
	}
		});
		dataSource.open("GET", url);
	}else{
		alert("Network not available. Check your settings.");
	};
};

exports.netCheck = netCheck;